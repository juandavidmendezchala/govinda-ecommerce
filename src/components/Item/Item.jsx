export default function Item({title, description, price}){
    return(
        <div>
            <h2>{title}</h2>
            <p>{description}</p>
            <p>Price:{Price}</p>    
        </div>
    )
}